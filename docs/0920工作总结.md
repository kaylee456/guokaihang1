# 工作总结

---

## 2026-09-20　M0 框架地基

### 一、今日目标

按《开发计划 v2.0》调整里程碑顺序（框架地基提到 M0 先行），并完成 M0 交付：**LLM 客户端 + Provider 抽象/registry + FastAPI 骨架**，目标 `/health`、`/llm/ping` 跑通。

### 二、完成事项

1. **开发计划调整**：阶段表中「框架地基」与「数据地基」对调，框架先行为 M0，数据顺延为 M1。
2. **M0 骨架落地**（`D:\国开行\`）：

   | 文件 | 作用 |
   |---|---|
   | `requirements.txt` | fastapi / uvicorn / httpx / pydantic / pydantic-settings / pyyaml / pytest |
   | `.env.example` | LLM / Provider / 端口配置模板 |
   | `README.md` | M0 说明 + 启动/验证步骤 |
   | `backend/config.py` | pydantic-settings 读 `.env`，进程级单例 |
   | `backend/app.py` | FastAPI 入口：`/health`、`/llm/ping`、`/llm/echo`、`/llm/stream` |
   | `backend/llm/client.py` | vLLM 兼容客户端：chat / stream / 思考开关 / 重试 |
   | `backend/capabilities/base.py` | Provider 抽象（C1..C10）+ DTO(`CapabilityResult`) |
   | `backend/capabilities/registry.py` | 按 `PROVIDER_MODE` 分发单例 |
   | `backend/capabilities/mock_provider.py` | 读 `mockdata/*.yaml`，缺数据优雅降级为 `missing` |
   | `backend/capabilities/db_provider.py` | 接库契约，本期 `NotImplementedError` 留 TODO |
   | `tests/test_smoke.py` | 不外呼 LLM 的装配自检 |

### 三、关键设计（对齐计划第七节）

- Qwen 思考开关走 `chat_template_kwargs={"enable_thinking": false}`；`max_tokens` 默认 2048，避免思考吃光 token 导致正文为空。
- 重试策略：仅对网络错误与 5xx 重试，4xx 直接抛出；流式不做整段重试（已开始输出无法回退）。
- Provider 各能力统一返回 `CapabilityResult`（`status`/`data`/`gaps`）；业务层只依赖 `get_provider()`，mock↔db 一键切换、业务代码零改动。

### 四、待办 / 遗留

- **本地未验证**：开发机（Windows）未安装 Python（`python` 为应用商店占位、退出码 49，无 `py` 启动器），冒烟测试与端点尚未实跑。需在服务器 `/data/liuqingting/guokahang/` 上执行：
  ```
  pip install -r requirements.txt
  cp .env.example .env
  python -m pytest tests/ -v
  python -m backend.app
  curl http://127.0.0.1:8000/health    # 期望 status:ok
  curl http://127.0.0.1:8000/llm/ping  # 期望 ok:true
  ```
- **安全提醒**：`/health`、`/llm/*` 端点目前无鉴权，内网联调够用；后续对外或跨网访问前需加访问控制。
- **下一步**：验证通过后进入 M1 数据地基（`schema/`、`mapping.md`、`mockdata/` 样例客户假数据）。
