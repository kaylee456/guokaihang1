# 国开行智能客户分析 Agent — 开发计划（定稿 v2.0）

> 版本：v2.0　定稿日期：2026-09-20
> 项目目录：`/data/liuqingting/guokahang/`
> 技术栈：Python 3.10+ / FastAPI / Uvicorn（与 `data-agent-skill` 对齐）
> LLM：`http://10.200.3.10:8025/v1`，模型 `/data/gc_llm/nlp/Qwen3.6-27B`（vLLM，OpenAI 兼容，max_model_len=131072）

---

## 一、现状盘点（前期已完成）

| 项 | 状态 | 说明 |
|---|---|---|
| SSH / 项目目录 | ✅ | 公钥登录，`/data/liuqingting/guokahang/` |
| LLM 连通 | ✅ | vLLM + Qwen3.6-27B，`content` 正常返回 |
| 技术栈 | ✅ | Python + FastAPI，新建独立骨架 |
| 真实数据字典固化 | ✅ | 44 表 / 1096 字段 → `schema/data_dictionary.json` + `summary.txt` |
| 能力↔真实表映射 | ✅ | 见第四节；C1/C4/C10 覆盖充分，C2/C5/C7/C8 有缺口 |

---

## 二、总体路线

一套「智能客户分析 Agent」，两层结构：

```
上层：3 个问题工作流（编排）   Q1 单客户诊断 · Q2 名单+融资 · Q3 关联图谱
下层：10 个原子能力（取数）    C1..C10，统一 Provider 接口
数据：本期 MockProvider（照真实表字段名造假数据）
      未来 DBProvider（接库），一键切换，业务层零改动
```

三个客户问题不是三套系统，而是同一批底层能力的三种组合。Q1 的能力包是可复用内核：Q2 = 名单过滤 + 批量跑 Q1-lite；Q3 = 关系扩散 + 逐节点跑 Q1 子集。

---

## 三、四个分析维度（能力基线）

1. **客户基本情况**：工商、主营、股东股权；财务（资产负债率/现金流/收入/利润）；我行业务（贷款/基金/存款/中间业务余额、信用评级、项目资产质量）
2. **经营财务状况**：经营分析（主营、重大项目投资、公司治理）；财务指标（偿债/盈利/运营能力）
3. **风险状况**：风险大数据平台预警、评级器重大风险事件、观察名单、征信提示
4. **客户管理情况**：准入、分类分级、客我供求关系、经营主责人、客户名单

---

## 四、基础能力 ↔ 真实表映射

主键（跨表 JOIN 连接键）：客户编号 `CST_ID`、组织编号 `ORG_ID`、集团编号 `GRP_ID`。

(1)客户业务数据信息：
两种方式：
1.TBCCPNM0参与人名称信息（从CST_FULLNM找到对应的ORG_ID）
2.客户资金信息统计表（RPT_CUST_CAPITAL_DETAIL）（从CST_NM找CST_ID，ORG_ID）

客户贷款信息统计表（RPT_CUST_LOAN_DETAIL）（CST_ID，ORG_ID）
客户存款信息统计表（RPT_CUST_DEPOSIT_DETAIL）（CST_ID，ORG_ID）
客户基金信息统计表（RPT_CUST_FUND_DETAIL）（CST_ID，ORG_ID）
客户担保信息统计表（RPT_CUST_GUARANTY_DETAIL）（CST_ID，ORG_ID）
客户中间业务信息统计表（RPT_CUST_MIDBUSINESS_DETAIL）（CST_ID，ORG_ID）

RPT_CUST_BS_DETAIL 客户业务数据详情表 (客户有无参与)（CST_ID，ORG_ID）（暂无此表）
RPT_CUST_BS_SUM	客户业务数据汇总表（分行汇总情况）（ORG_ID）（暂无此表）

(2)客户信用评级：
C_CUST_CREDIT_INFO（客户信用评级表） （CST_ID）
分类分级：
C_CUST_CLASSIFY_INFO（客户分类分级主表）(CST_ID)

（3）客户管理情况：
C_CUST_SUPPLY_RELA（客我供求关系表)(CST_ID, RELA_ID)（客户对业务的意愿的强度，高度我求人、人求我）
C_CUST_SUPPLY_RELA_DETAIL（客我供求关系详情表）(DETAIL_ID, CST_ID)  （待确定）
C_CUST_MAIN_MGR_INFO（客户经营主责人主表）(CST_ID)  （负责重点客户的责任人，重点是找到职务（分行副行长。。。），负责多少客户）

（4）客户准入数据：
C_CUST_ADMISSION（客户准入认定信息表）(CST_ID) 

（5）客户名单管理：
C_CUST_TAG_INFO 查询“观察者名单”的标识id  (TAG_ID, TAG_NAME )  名单的定义
C_CUST_TAG_REL   查询在该名单中的客户有哪些。(CUST_ID, TAG_ID)   名单与客户的关系

（6）客户经理与客户关系：
C_CUST_TEAM_INFO	客户管理团队信息表    定义了一个客户的管理小组
C_CUST_TEAM_MEMBER	客户管理团队成员信息表   小组+客户+成员   根据客户经理决定所属分行，客户经理所在的分行即客户所属分行 （客户经理A（主管）、B(备用)）

（7）归口管理部门：
归哪个总部哪个部门统管，实际管理者是分行
C_CUST_MANAGE_DEPT  

（8）机构：
CCBINS_INF	机构信息
CCBINS_REL	开行机构关系

（9）员工表：
EMPE	员工信息

（10）客户基本情况：
（TB开头：多实体+ORG_ID）
TBCCCUS0       客户  
C_CUST_INFO_BC  客户基本信息补录表
TBCCA060	 客户其他信息
TBCCOIP0	 参与人
TBCCPAD0        参与人邮政地址
TBCCPID0	         参与人识别信息
TBCCPLR0	 参与人和位置关系
TBCCPNM0       参与人名称信息 
TBCCPPR0	 参与人和参与人关系 （多实体+ORG_ID）

（11）股东信息：
##TBCCINV0	 投资人 
TBCCCFD0	 投资人出资信息
TBCCLST0	         组织上市情况
TBCCOII0	         组织行业信息

（12）集团客户表：
C_BLOC_CLASSIFY_INFO	集团分类分级主表
C_BLOC_MAIN_MGR_INFO 集团经营主责人主表
C_BLOC_MANAGER	集团管户信息表 （集团业务主管-客户经理）
TBCCA160	集团信息表 （主表）
TBCCA170	集团成员客户信息 
TBCCA180	集团管理信息

（13）自然人（公司法人。。）：
TBPC1010	客户基本信息	
TBPC1510	客户联系位置信息	
TBCCPSR0	组织和关联人关系

（14）中心的汇总信息：
B_BATCH_INFO	批次表
B_BUSINESS_STATISTICS	业务统计表
B_CREDIT_RATING_DIST	信用评级结果情况分布表
B_CUST_LIFECYCLE_INFO	客户生命周期表
B_CUST_NEW_INFO	新增客户信息表
B_CUST_SMART_ANALYSIS_LOG	客户信息智能分析记录表
B_CUST_STOCK_INFO	客户存量信息表
B_CUSTOMER_CLASSIFY_GRADE_DIST	客户分类分级表
B_CUSTOMER_LIST_DIST	客户名单情况表
B_CUSTOMER_OWNER_DIST	客户经营主责人分布表
B_CUSTOMER_SUPPLY_DEMAND_DIST	客我供求关系分布表
B_ENTERPRISE_SCALE_DIST	企业规模表
B_INDUSTRY_DIST_INFO	行业分布表
B_OWNERSHIP_TYPE_DIST	所有制性质表

（15）财务数据:
已接入（5张表）

（16）审计数据：
待接入

（17）项目资产质量：
缺失

（18）风险数据：
缺失

（19）征信数据：
缺失

（20）码值对应：（需要表-字段-字典类型）(缺失)
system_dict_data	字典数据表
system_dict_type	字典类型表


## 五、目录结构

```
/data/liuqingting/guokahang/
├── README.md
├── SKILL.md                    # 能力契约声明（占位）
├── requirements.txt
├── .env.example                # LLM_BASE_URL / LLM_MODEL / PROVIDER_MODE / 端口
├── schema/                     # ✅ 已生成：真实数据字典（单一事实来源）
│   ├── data_dictionary.json
│   └── summary.txt
├── backend/
│   ├── app.py                  # FastAPI 入口
│   ├── config.py
│   ├── llm/client.py           # Qwen 兼容客户端（chat/stream/enable_thinking/重试）
│   ├── capabilities/
│   │   ├── base.py             # Provider 抽象 + DTO(pydantic)
│   │   ├── registry.py         # mock/db 选择分发
│   │   ├── customer_info.py … relation.py   # C1..C10
│   │   └── mapping.md          # 能力↔表↔字段映射 + 缺口标注
│   ├── workflows/
│   │   ├── q1_diagnosis.py
│   │   ├── q2_namelist.py
│   │   └── q3_relation.py
│   ├── prompts/                # 分维度 & 综合 提示词模板
│   └── report/render.py        # 结构化结果 → Markdown 报告
├── mockdata/                   # 2-3 样例客户全维度假数据（照真实字段名）
└── tests/
```

---

## 六、阶段安排（约 7 个工作日）

| 阶段 | 内容 | 交付物 | 工期 | 依赖 |
|---|---|---|---|---|
| **M0 框架地基** | LLM 客户端（流式/重试/思考开关）、Provider 抽象+registry、FastAPI 骨架 | `/health`、`/llm/ping` 跑通 | 1 天 | — |
| **M1 数据地基** | 字典→schema（已半成）、`mapping.md`、造 2-3 样例客户全维度假数据（照真实字段名） | `schema/`、`mapping.md`、`mockdata/` | 1 天 | — |
| **M2 Q1 单客户诊断** | 四维取数→分维度提示词→LLM 综合→合作+风控建议报告（**关键路径**） | `POST /analyze/q1` 出完整报告 | 2 天 | M0/M1 |
| **M3 Q2 名单+融资** | 名单/标志位筛选（注册地+行业）→批量 Q1-lite→融资建议+风险点 | `POST /analyze/q2` | 1 天 | M2、名单表待补 |
| **M4 Q3 关联图谱** | 集团/股东/关联扩散（`GRP_ID`/`SUPR_CST_ID`）→审计上下游→逐主体补数→关联度报告 | `POST /analyze/q3` | 1.5 天 | M2 |
| **M5 收口** | 三问题端到端回归、报告模板打磨、`DBProvider` 接入契约与文档 | 回归通过、接库路径清晰 | 0.5 天 | M2-M4 |

> M2 是核心路径，打通后 Q2/Q3 大量复用其能力包与提示词。

---

## 七、关键技术要点

- **LLM**：Qwen3.6 是推理模型，`max_tokens` 要给足（思考吃 token，否则正文被截断为空），分析类调用 ≥ 2048；需要关思考传 `chat_template_kwargs:{"enable_thinking":false}`；抽取用低温(0.1-0.3)，综合建议略高(0.4-0.6)；analyze 超时建议 120s，含 2 次重试；前端进度用 SSE 流式。
- **Provider 抽象**：`PROVIDER_MODE=mock|db` 由 `.env` 控制，registry 统一分发。`MockProvider` 读 `mockdata/*.yaml`；`DBProvider` 留 TODO 契约，未来接库。
- **mock 即 schema 契约**：假数据照真实表名/字段名造，DTO/SQL/报告面向真实字段编程，接库零改动。
- **取数走固定 SQL**：C1-C10 是已知固定查询，用参数化 SQL/视图，不用 NL→SQL（`vanna` 留给未来"模板外临时提问"兜底）。
- **报告**：Markdown 主体（可转 PDF/Word）+ 结构化 JSON 供前端渲染。

---

## 八、待确认（影响进度）

1. **缺口表**：财务报表(C2)、征信(C7)、名单管理(C8：科创/战略优质/研发贷款)、评级器(C5) 不在当前 44 表内，需补充表结构，否则先用 mock 占位。
2. **Q2 名单来源**：先用"标志位(`ST_TP_SME_IND`)+行业+注册地"临时模拟，还是等名单表？
3. **报告模板**：合作/风控/融资建议是否有行内既定输出结构需对齐。

---

## 九、本期边界与下期衔接

**本期做**：Agent 编排内核、三类问题工作流、十大能力接口、贴真实字段的 mock 数据、报告生成、LLM 客户端。

**本期不做（留契约）**：真实 skills（Provider 用 mock）、数据库接入（`DBProvider` 留 TODO）、审计 PDF 实际解析（可复用同目录 `pdf-ocr-service`）、前端界面（以 API + Markdown 交付）。

**下期衔接**：`MockProvider`→`DBProvider` 一键切换；风险预警 C4 对接 `bigdata-platform`(qdata)；模板外提问经 `vanna` NL→SQL 兜底。
